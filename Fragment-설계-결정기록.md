# Fragment 설계 결정 기록 — 관계 그래프 시각화와 탐색 UI

기간: 2026-09-17 ~ 2026-09-19 (H02 성능 확인 작업 중 진행)
목적: 최종 보고서에서 "어떤 선택지 중 무엇을, 왜 골랐는지"를 근거와 함께 설명하기 위한 기록이다. 결론보다 판단 과정을 남긴다. 틀렸던 가정과 방향을 바꾼 지점도 그대로 적는다.

## 한눈에 보기

| # | 결정할 문제 | 검토한 선택지 | 최종 선택 | 결정 근거 |
| --- | --- | --- | --- | --- |
| 1 | 대규모 그래프 배치 | circle, concentric, cose, fcose, breadthfirst(트리·방사형), cise, dagre | **fcose + 허브 분산 설정** | 실측(시간·화면 멈춤)과 8가지 배치 캡처 비교 |
| 2 | 노드·글자가 겹쳐 읽기 어려움 | 간격만 넓히기 / 크기·라벨·관계 이름 정리 | **크기는 연결 수 비례(10~32px), 라벨은 확대 단계별, 최소 간격은 계산으로 보장** | 겹침 원인을 셋으로 나눠 각각 해결하고 좌표로 검증 |
| 3 | 사용자가 무엇을 고르게 할지 | 배치 방식 선택 메뉴 / 보는 범위 선택 | **보는 범위 선택(중심 개념 + 1·2단계 이웃)** | 캡처 비교 결과, 보기 좋은 그림은 배치가 아니라 범위를 좁혔을 때 나왔다 |
| 4 | 중심 개념 고르는 방식 | 선택 상자 / 브라우저 자동완성 / 직접 만든 목록 | **직접 만든 검색 목록(콤보박스)** | 기본 자동완성 창은 크기·모양을 바꿀 수 없음 |
| 5 | 조작 요소의 모양 | 브라우저 기본 모양 / 통일 규칙 | **높이·모서리·글자·화살표·× 통일** | 같은 화면의 요소가 제각각이면 완성도가 떨어져 보임 |
| 6 | 관계 유형 구분 | 화살촉만 / 화살촉+선 모양 / 화살촉+선 모양+색 | **화살촉 + 선 모양** | 같은 배치에서 4가지를 캡처 비교해 사용자가 선택 |

## 1. 대규모 그래프 배치: "왜 원형이었나"에서 시작

**출발점.** 500개 카드·1,500개 관계로 성능을 확인하던 중(H02), 그래프가 원형으로 그려지는 것이 눈에 띄었다. 코드를 확인해 보니 노드가 80개를 넘으면 `cose`(힘 기반 배치) 대신 `circle`을 쓰도록 되어 있었다(`5cbe8fb`부터 있던 동작).

**추측하지 않고 재봤다.** 원형을 쓰는 이유가 속도라는 가정을 실제로 확인했다.

| 배치 | 500개 그래프 표시까지 | 비고 |
| --- | --- | --- |
| circle | 303ms | 한 줄짜리 원이라 정보가 없음 |
| concentric | 349ms | 연결 수 기준 고리. 가독성 낮음 |
| cose | **3,607ms** | 계산 중 페이지 전체가 멈춤(스크롤·클릭 불가) |

단순히 느린 것이 아니라 **화면이 멈춘다**는 점이 핵심이었다. 그래서 "원형이냐 힘 기반이냐"의 문제를 **"힘 기반 배치를 멈춤 없이 할 수 있느냐"**의 문제로 다시 정의했다.

**참고 대상을 조사해 문제를 다시 봤다.** 참고한 GraphRAG의 그래프가 보기 좋은 이유가 LLM(GPT-4 Turbo) 때문인지 확인했다.
- LLM은 문서에서 개체와 관계를 **뽑아 그래프 데이터를 만드는 단계**에 쓰인다.
- 화면 배치는 Gephi의 **ForceAtlas2**(힘 기반)이고, 노드 크기는 연결 수, 색은 군집이다.
- 결론: LLM이 없어서 생긴 한계가 아니었다. 배치 방식과 데이터 모양의 문제였다.

**테스트 데이터가 결론을 왜곡하고 있었다.** 처음 fixture는 무작위 연결이라 모든 카드의 연결 수가 비슷했고, 퍼져 나갈 "허브"가 없었다. 어떤 배치를 써도 털뭉치가 될 수밖에 없는 데이터였다. 그래서 실제 지식 그래프처럼 연결이 소수에 몰리도록 fixture를 바꿨다(선호적 연결, 같은 카테고리끼리 85%). 결과는 상위 허브 연결 30~41개, 중앙값 4개였다. **평가 도구가 틀리면 결론도 틀린다**는 점을 확인한 지점이다.

**허브를 서로 떨어뜨리는 방법.** 허브끼리 오히려 한곳에 뭉치는 현상이 있었다. 허브는 선이 많아 이웃에게 강하게 끌려가고, 이웃을 공유하는 허브끼리 붙기 때문이다. ForceAtlas2의 *Dissuade Hubs* 개념을 fcose 설정으로 옮겨 해결했다.
- 연결이 많을수록 밀어내는 힘을 키운다.
- 허브에 붙은 선은 당기는 힘을 연결 수만큼 약하게 한다.
- 허브끼리 잇는 선은 길게 잡는다.

**여러 후보를 캡처로 한 번에 비교했다.** 말로 설명하며 한 방향씩 시도하는 대신, 같은 데이터와 스타일로 8가지를 그려 나란히 봤다.

| 배치 | 계산 시간 | 판단 |
| --- | --- | --- |
| fcose (힘 기반) | 586ms | ✅ 카테고리별 영역이 나뉘고 허브가 보임 |
| 계층형 트리 (breadthfirst) | 63ms | ❌ 층이 2~3개뿐이라 가로줄로 납작해짐 |
| 방사형 트리 | 51ms | ❌ 결국 동심원 고리 |
| concentric | 37ms | ❌ 동심원 고리 |
| 카테고리 군집 원형 (cise) | 28초 | ❌ 너무 느리고 한 줄로 뭉개짐 |
| 레이어형 (dagre) | 1.3초 | ❌ 세로 띠로 뭉개짐 |
| 중심 개념 + 2단계, 트리 | 34ms | ❌ 가로줄로 납작해짐 |
| 중심 개념 + 2단계, fcose | 380ms | ✅ 가지가 뻗고 이름이 읽힘 |

트리·레이어 계열이 실패한 이유: 관계가 촘촘하면 거의 모든 카드가 허브에서 2~3단계 안에 들어온다. 그래서 층이 적고 한 층에 수백 개가 몰린다. **트리 배치는 데이터가 트리 모양일 때만 예쁘다.**

**결정:** 규모와 상관없이 fcose 하나로 통일하고 80개 분기를 없앴다. 카드 수에 따라 그림의 성격이 갑자기 바뀌지 않는다.

## 2. 가독성: 겹침의 원인을 나눠서 하나씩

"보기 어렵다"를 한 문제로 보지 않고 원인 셋으로 나눴다.

| 원인 | 해결 |
| --- | --- |
| 노드 원이 모두 크다(28px) | 연결 수에 따라 10~32px. 제곱근 비율이라 허브도 과하게 커지지 않는다 |
| 이름 500개가 항상 떠 있다 | 멀리서는 연결 수 상위 개념만, 확대하면 전부 표시한다. 항상 보이는 이름은 축소해도 화면에서 11px을 유지한다 |
| 관계 이름 1,500개가 선 위에 있다 | 평소에는 숨기고, 강조한 노드의 선에만 표시한다 |

**간격은 감이 아니라 계산으로 정했다.** 기준은 "가장 큰 노드 둘이 나란히 있어도 여유가 남는 거리", 즉 32 + 32 + 24 = **88px**이다. 힘 기반 배치는 목표 거리 근처로 끌고 갈 뿐 보장하지는 않는다. 그래서 배치 후 88px보다 가까운 쌍을 밀어내는 보정 단계를 따로 두었다.
- 처음 방식(모든 쌍 비교)은 극단적인 밀집에서 300~900ms가 걸렸다. 격자 칸으로 나눠 이웃 칸끼리만 비교하도록 바꿔 68~195ms로 줄였다.
- 실제 앱 화면의 좌표를 꺼내 검증했다. 500개·260개·42개 화면 모두 최소 중심 간 거리 88px, 겹치는 노드 쌍 0건이었다.

## 3. 배치를 고르게 할까, 범위를 고르게 할까

원형이 계속 마음에 들지 않자 "사용자가 배치를 직접 고르게 하면 어떨까"라는 아이디어가 나왔다. 캡처 비교(1번)를 해 보니 쓸 만한 배치는 fcose 하나였다. 또 **가장 보기 좋은 그림은 배치를 바꿨을 때가 아니라 보는 범위를 좁혔을 때** 나왔다.

**결정:** 배치 선택 메뉴는 만들지 않았다. 사용자가 고르는 것을 "어떻게 그릴지"에서 **"어디를 얼마나 넓게 볼지"**로 바꿨다.
- 중심 개념을 고르면 **이웃 범위(1단계/2단계)**를 선택할 수 있다.
- 1단계는 허브에서 이웃이 뻗어 나가는 모양이다. 처음 원했던 "뻗어가는 구조"에 가장 가깝다.
- 그래프 영역도 480px 고정에서 화면 높이의 70%(480~820px)로 늘렸다.

## 4. 중심 개념 고르기: 세 번의 선택

| 단계 | 방식 | 문제 | 다음 선택의 이유 |
| --- | --- | --- | --- |
| 1 | 선택 상자 | 500개 중에서 스크롤로만 찾아야 함 | 검색이 필요 |
| 2 | 브라우저 자동완성(datalist) | 목록이 입력창보다 작고 말풍선 모양. **CSS로 바꿀 수 없음** | 모양을 제어할 수 있어야 함 |
| 3 | 직접 만든 검색 목록 | — | 채택 |

최종 동작은 다음과 같다.
- 목록은 입력창과 **같은 폭·테두리·모서리**로 바로 아래에 펼쳐진다.
- 항목 오른쪽에 **카테고리를 흐리게** 표시한다.
- 이름·별칭으로 걸러지고, 입력한 글자로 시작하는 이름이 위에 온다. 최대 50개까지 보여주고 나머지는 개수로 안내한다.
- ↑↓·Enter·Esc 키보드 조작과 스크린리더용 콤보박스 표준 속성을 지원한다.

## 5. 조작 요소 통일

같은 화면의 입력창, 선택창, 버튼이 브라우저 기본 모양으로 제각각이었다. 파란 ×, 검은 ▼, 서로 다른 높이였다. 규칙 하나로 맞췄다.
- 높이 46px, 모서리 9px, 글자 15px
- 화살표는 작성 폼에 이미 있던 꺾쇠 모양을 기준으로 삼고 항상 오른쪽 끝에 둔다. ×는 같은 선 굵기로 화살표 왼쪽에 둔다.
- ×는 글자가 있을 때만 보이고, 누르면 입력을 지운 뒤 커서를 남긴다.
- 밝은·어두운 테마, 390px 모바일, 윈도우 고대비 모드까지 확인했다.

## 6. 관계 유형 구분: 화살촉과 선 모양

관계는 네 유형(관련·선행·상위·비교)인데, 그래프에서는 선행과 상위가 같은 삼각형이고 관련과 비교는 둘 다 화살표 없는 실선이었다. 관계 이름은 강조했을 때만 보이므로 평소에는 유형을 알 수 없었다.

같은 중심 개념·같은 노드 배치에서 스타일만 바꿔 네 후보를 캡처했다.

| 후보 | 관련 | 선행 | 상위 | 비교 | 판단 |
| --- | --- | --- | --- | --- | --- |
| 현재 | 실선 | ▶ | ▶ | 실선 | 선행·상위, 관련·비교가 구분되지 않음 |
| A. 화살촉만 | 실선 | ▶ | ◇ | ⊣⊢ | 화살촉이 작고 흐려 멀리서는 차이가 안 보임 |
| B. 화살촉 + 선 모양 | 실선 | ▶ 실선 | ◇ 긴 점선 | ⊣⊢ 점선 | ✅ 선택 |
| C. B + 유형별 색 | 회색 | 갈색 | 초록 | 진홍 | 가장 잘 보이지만 노드의 카테고리 색과 색 의미가 두 겹이 됨 |

**결정:** 사용자가 B를 골랐다. 색은 이미 노드의 카테고리를 뜻하므로, 선에는 색 대신 모양으로 의미를 싣는다. 상위 개념의 ◇는 UML에서 "부분-전체"에 쓰는 마름모를 빌렸다.

**남은 점:** 허브 노드 가장자리에 화살촉이 몰리면 겹친다. 범례가 없어 처음 보는 사람은 모양의 뜻을 알기 어렵다.

## 이번 과정에서 지킨 원칙

1. **추측보다 측정.** "느려서 그럴 것"이라는 가정을 실측(3,607ms, 화면 멈춤)으로 확인한 뒤 문제를 다시 정의했다.
2. **참고 대상은 겉모습이 아니라 원리를 본다.** GraphRAG의 그림이 좋은 이유를 조사해, LLM이 아니라 배치 방식과 데이터 모양 때문임을 확인했다.
3. **평가 도구부터 의심한다.** 무작위 fixture로는 어떤 배치도 좋게 나올 수 없었다. 현실적인 데이터로 바꾼 뒤에야 비교가 의미를 가졌다.
4. **말보다 그림으로 비교한다.** 8가지 배치를 캡처로 나란히 놓자 한 번에 방향이 정해졌다.
5. **"보기 좋다"를 검증 가능한 기준으로 바꾼다.** 겹침은 "최소 거리 88px·겹침 0건"으로, 목록 모양은 "입력창과 같은 폭"으로 정의하고 실제 화면에서 측정했다.
6. **안 되는 것은 안 된다고 기록한다.** 브라우저 기본 자동완성은 꾸밀 수 없다는 한계를 확인하고 직접 만드는 쪽으로 바꿨다.

## 남은 한계와 후속 과제

- 500개 전체 보기는 여전히 원반형이다. 관계가 촘촘한 그래프의 성질이며, 범위 선택으로 보완한다.
- 멀리서 볼 때 가까이 붙은 허브 두 개의 **이름끼리** 닿는 경우가 두어 군데 있다(노드 겹침은 없음).
- 브라우저 테스트를 돌리면 테스트용 Astro 서버가 공용 콘텐츠 캐시(`.astro/`)를 덮어써서, 실행 중인 fixture 서버가 다른 카드를 보여주는 **테스트 환경 간섭**이 있다. CI는 단계를 순서대로 실행해 영향이 없다.
- 위 계산 시간은 비교용 캡처 스크립트의 측정값이다. 최종 성능(검색 p95 200ms, 그래프 2초 목표)은 H02 재측정으로 확정한다.
