# 취직준비 Helper Agent — One Pager

**6대 점검 항목 색인** — Goal §2 · Input §5 · Tools §8 · State §9 · Output §5,§10 · Failure §11

---

## 1. 개요

|항목|내용|
|---|---|
|Agent 이름|취직준비 Helper Agent|
|한 줄 소개|지원할 회사·부서를 지정하면, 그곳이 요구하는 역량·인재상을 수집해 내 역량과 비교하고 **남은 기간 내에 무엇을 내세우고·채우고·포기할지** 전략을 짜주는 Agent|
|타겟 사용자|특정 회사·직무를 목표로 준비 중인, **시간이 촉박한** 취업준비생 및 이직 희망자|
|핵심 가치|"무엇이 부족한지"에서 끝나지 않고 **"주어진 기간에 어디부터 손대는 게 가장 이득인지"** 를 알려준다|

## 2. 문제 정의와 목표 (Goal)

취준생은 채용공고를 읽어도 자신의 상태와 대조하지 못한다. 공고의 "쿠버네티스 경험"과 자신의 "Docker로 개발환경 구성해본 경험"이 얼마나 먼 거리인지 판단할 기준이 없고, 준비의 우선순위를 세우지 못한 채 막연히 학습한다.

**실제 취업 시장의 작동 방식**: 모든 역량을 다 갖추고 지원하는 사람은 없다. 강점을 부각하고 부족분은 채워가며 지원한다. 그리고 대부분 **시간이 촉박하다.** 따라서 "부족한 것을 다 채워라"는 조언은 현실에서 무력하다.

**Goal.** 사용자가 **지원 목표 시점까지 남은 기간 안에서** 준비의 우선순위를 세우게 하는 것. 부족분 나열이 아니라 **내세울 것 / 채울 것 / 포기할 것**의 트리아지에 답한다.

**완료 조건.** 요구 역량이 근거와 함께 판정되고, 각 항목이 세 트랙(강점·기간 내 보완·이번엔 제외) 중 하나로 분류되며, 보완 항목에 실행 가능한 순서가 붙으면 1회 분석이 완료된 것으로 본다.

**왜 Agent여야 하는가.** 회사·부서마다 정보가 흩어진 위치와 형태가 다르다. 검색어를 바꿔가며 찾고, 결과가 부실하면 다른 각도로 재시도하고, 언제 충분한지 스스로 판단하는 수집 과정은 고정 파이프라인으로 구현할 수 없다. 이 판단이 시스템을 Agentic 하게 해줌.

## 3. 핵심 유스케이스

**UC-1 (프로필 구축 모드)** — 이력서·경력기술서 업로드 또는 블로그·GitHub URL 제출 → 역량을 원자 단위로 추출 → **직군 프리셋 기반 표준 질문으로 내 레벨 좌표를 측정**(회사 무관, 1회) → 프로필 JSON 저장

**UC-2 (회사 분석 모드)** — "현대오토에버 / 백엔드" + **지원 목표 시점** 입력 → **`discover_jobs`로 해당 회사의 열린/과거 공고를 열거하고 대분류·직무코드로 그룹핑해 제시 → 사용자가 구체 공고(들)를 선택**(모호성 해소 체크포인트; 사용자가 이미 특정 공고 URL/본문을 준 경우 생략) → 선택 공고 기준 요구 역량·인재상 수집 → 저장된 프로필과 매칭 → **JD에서 뽑은 기준으로 검증이 필요한 항목만 델타 인터뷰** → **지원 전략 브리프** 출력 (공고를 여럿 고르면 각각 분석 후 궁합 비교)

_**델타 인터뷰란? 사용자가 적은 내용이 애매하거나 판단 근거가 더 필요한 경우 Agent가 추가적으로 사용자에게 인터뷰/질문/퀴즈 를 통해서**_

> UC-1은 "내 좌표를 찍는 일"(회사 무관), UC-2는 "그 좌표가 이 회사 기준선을 넘는지 보는 일"(회사 종속). 둘을 잇는 것은 **§7의 공유 계약**이다. 소스가 달라도 같은 규격으로 정제되기에 매칭이 성립한다. 두 번째 회사부터는 프로필이 채워져 있어 델타가 거의 없다.

![uc1_uc2_flow.png](attachment:2b3391f7-efdd-47d9-9315-5cb565261302:uc1_uc2_flow.png)

## 4. 범위

**In Scope (v1)**

- 요구 역량·인재상 수집 및 구조화 (JD 중심)
- 내 역량 프로필 구축 (파일 업로드 + OCR + 대화형 레벨 측정)
- 역량 gap 분석 (기준 분해 기반 3-state 판정)
- 강점 식별 및 회사 언어로의 번역
- **기간 제약 하의 지원 전략 트리아지 + 학습 순서 제시**

**Out of Scope (v2 이후** — 나중에 더 확장해보는 방향으로 진행**)**

- 채용 절차·전형 안내 — 소스(합격 후기)가 완전히 달라 별개 제품. **v2 최우선 후보**
- 여러 회사 동시 비교 — 프로필 저장이 있으므로 저비용 추가 가능
- 자소서 작성 (프롬프트로 자소서 틀을 주는것도 괜찮을 듯 함)
- **수집 도구의 MCP 서버화** — v1에서는 파이썬 함수로 충분. 확장 시점에 서버화
- **캐시/RAG** — 콘텐츠 규모가 작아 v1 불필요 (단순히 배운 내용을 써보고자 해서 쓸까 싶었는데.. 애매함)

**의도적으로 하지 않는 것(제외한 항목들)**

- LinkedIn 프로필 수집 — 공식 API 부재, ToS가 스크래핑 금지
- 성향 진단 — 검증된 심리측정 도구 없이 만들면 신뢰도 없음. 진단 대신 경험에서 **추출**만 한다
- 컬처핏 시험 공략법 — 합격해도 본인이 불행해지는 조언. 절대 생성하지 않는다
- 학습 소요 기간의 절대 시간 추정 — 개인차가 커 근거가 약함. 상대 순위 + 구간(단기/중기/장기)으로만 표현

## 5. 입력과 출력 (Input / Output)

**Input**

|무엇|형태|필수 여부|
|---|---|---|
|회사명 + 부서/직무|텍스트|필수|
|**지원 목표 시점**|날짜|**필수** (트리아지·우선순위의 근거)|
|내 역량 근거|이력서·경력기술서 파일, 블로그·GitHub URL|최소 1개|
|레벨 측정 / 기준 검증 답변|`st.form` 배치 질문에 대한 응답|조건부|
|기존 프로필|프로필 JSON 업로드|선택 (있으면 인터뷰 대폭 축소)|

**Output** — 지원 전략 브리프. **자유 서술이 아니라 코드가 골격을 소유하는 고정 스키마(`StrategyBrief` 출력 계약)**로, 같은 입력이면 같은 골격·계산 결과가 나온다(멱등성). LLM은 슬롯 안 자연어만 채운다. 스키마·필수 필드는 §10.

## 6. 데이터 소스

|등급|소스|최소 요건|
|---|---|---|
|Primary|채용공고(JD), 현재 + 과거 — **발견은 공식 API**(사람인·원티드·잡코리아·공공데이터), **본문은 3 Layer Fallback**|**UC-2 하드 요건: 최소 1건** (사용자 입력 경로로 항상 충족 가능)|
|Secondary|회사 기술블로그 글쓴이, 컨퍼런스 발표자, GitHub org, 채용 브랜딩 인터뷰|소프트 (없으면 리포트에 "미수집" 표기)|
|인재상|공식 인재상 페이지, 채용 홈페이지, 지속가능경영보고서|소프트 (없으면 컬처핏 분석 생략 표기)|
|Best-effort|취업박람회·채용설명회 자료|없어도 실패하지 않게 설계|

**수집 방식** : **발견(discovery)은 공식 채용 API**(사람인·원티드·잡코리아·공공데이터포털)를 1차 경로로 쓴다 — 실시간 web_search는 Fallback. API가 재현적·구조적 발견을 주므로 시연 시 검색 품질 편차가 사라진다.

**단, API는 메타데이터 + 표준 URL + 키워드 태그만 주고 요구사항 본문은 주지 않으므로**, JD 본문은 3 Layer Fallback으로 확보한다:

① 사용자 입력(붙여넣기/지정 URL — 법적 무결·전문 보장, 하드 게이트 백본) →

② 회사 자체 채용페이지(robots.txt 준수) →

③ 플랫폼 상세페이지 fetch(best-effort, robots.txt 준수, **제품이 의존하지 않음**)

**캐시는 두지 않는다** — 회사당 콘텐츠 규모(JD 몇 건 + 블로그 몇 편)가 작아 이득이 없고 TTL 관리 비용만 발생. 단, 지속가능경영보고서 등 통짜 대형 문서가 걸려 수집 총량이 컨텍스트 한계를 넘는 경우에만 조건부로 RAG 청킹을 적용한다.

![jd_collection_pipeline.png](attachment:9ecebd12-e681-4fd0-bff6-e979e24a24a6:jd_collection_pipeline.png)

## 7. 핵심 Layer 공유 계약과 매칭 파이프라인

### 7-1. 두 Layer의 데이터 계약 (모든 도구가 이 규격만 반환)

```
[Layer-1] SourceDocument  ← 모든 수집 도구의 공통 반환 형태
{
  출처유형   : JD | 기술블로그 | 인재상 | 발표 | 기타   (← 코드가 결정: 어느 도구가 가져왔는가)
  회사, 부서 : 문자열                                  (← 코드가 검증)
  제목, URL  : 문자열
  게시일     : 날짜                                    (← 가능하면 코드가 파싱)
  수집일     : 날짜                                    (← 시스템 시각, 코드가 채움)
  원문       : 텍스트
  추출키워드 : 배열                                    (← 결정론적 키워드 추출)
  신뢰도등급 : 상 | 중 | 하                            (← 출처유형별 규칙)
}

[Layer-2] CompetencyRecord  ← SourceDocument에서 추출한 역량
{
  대분류   : 직군 프리셋 3~5개 + 공통 "인재상·컬처핏"
  역량명   : 원문 표현 그대로 (일반화·병합 금지)
  중요도   : 필수 | 우대
  레벨     : 학습함 | 써봄 | 실무 운영 | 설계·주도
  근거     : { 출처명, URL, 인용 원문, 수집일 }        (← Layer-1 문서를 가리킴)
}
```

**원칙**: Layer-1 필드 중 LLM이 아니라 **코드가 채울 수 있는 것은 코드가 채운다**(출처유형·회사검증·수집일·키워드·신뢰도). **정제 책임을 모델에 넘기지 않아야** foundation 모델이 바뀌어도 결과가 흔들리지 않고, 이후 타입 체크·필수항목 체크가 가능해진다.

### 7-2. 매칭 = 3단계 (큰 판단 하나 → 감사 가능한 소판단 여럿)

```
1) 후보쌍 검색 (정량·결정적)
   요구/보유 역량 임베딩 → 유사도로 후보쌍만 선별
   ※ 유사도는 "후보 추림"까지만. 판정에는 쓰지 않음 (가짜 정밀도 방지)

2) 기준 분해 + 검증 (닐 튜터님 제안 반영)
   "쿠버네티스 활용능력 상" → 체크 가능한 기준 3~5개로 AI가 분해
     예: ①클러스터 운영 ②장애 대응 ③매니페스트 작성 ④운영 규모
   프로필로 판정 안 되는 기준만 → 질문 생성 → 델타 인터뷰(UC-2)
   각 기준: 충족 | 부분 | 미충족  (AI, 근거 인용 필수)

3) 결정적 집계 (AI 아님, 규칙)
   필수 기준 전부 충족 → 충족 / 필수 절반 이상 → 인접 / 그 외 → 미보유
```

**설계의 핵심은 `인접` 상태.**

> Docker 경험자에게 "K8s 미보유"는 부정확하다. "오케스트레이션만 얹으면 된다"가 진짜 유용한 정보이며, **시간이 촉박할수록 인접 항목이 최우선 보완 대상**이 된다.

### 7-3. 강점 식별

충족 항목 중 **내 레벨이 요구 수준을 상회하는 것**을 `강점`으로 표시한다. 별도 기계장치 없이 레벨 비교만으로 산출되며, 이력서·자소서에서 내세울 무기가 된다.

**상회 항목이 없을 때(공백 Fallback)**: 미달 항목을 강점으로 재라벨링하지 않는다(지어내지 않기, §1 철학). 대신 **트랙2(기간 내 채울 것)에서 이미 계산되는 최우선 순위 항목으로 시선을 유도**한다 — 새 판정을 만드는 게 아니라 이미 있는 우선순위를 앞당겨 언급하는 것뿐이다. 렌더링 세부는 §10.

> **두 가지 불변 규칙

1.** 역량은 원자 단위로 원문 보존한다. "컨테이너 관리 능력"으로 요약하는 순간 정보가 파괴된다.  
2. 모든 추출·판정에 근거를 붙인다. 최종 등급은 LLM이 뱉는 숫자가 아니라 **감사 가능한 소판정들의 산술**이다.

## 8. 아키텍처와 도구 (Tools)

```
[Streamlit UI]
      │
      ├── 프로필 구축 루프 (ReAct)   ── OCR ──> 이력서/포트폴리오
      │                              └─ st.form 레벨 측정
      │
      └── 회사 분석 루프 (ReAct)
                │
                ├── 수집 도구 (파이썬 함수, v1) ─→ SourceDocument[]
                │     discover_jobs(공식 API 발견) / fetch_jd_body(3층 Fallback)
                │     fetch_tech_blog / get_company_values
                │        └ 결정론적 후처리(출처유형·키워드·신뢰도 부여)
                │
                ├── extract_competencies  → CompetencyRecord[]
                ├── match_competencies    → 3단계 파이프라인(§7-2)
                └── build_strategy_brief  → 3트랙 트리아지(§10)
```

**패턴**: ReAct 단일 에이전트. 통제는 흐름이 아니라 **도구 경계**에 건다 — 도구는 "한 번 부르면 의미 있는 산출물이 나오는" 단위로 묶고 §7의 계약을 출력 스키마로 강제한다.

|도구|입력|출력|
|---|---|---|
|`discover_jobs`|회사, 부서|공식 API 발견 → SourceDocument[] 메타(출처유형=JD, 표준 URL 포함)|
|`fetch_jd_body`|JD URL 또는 사용자 입력|JD 본문 텍스트 (①사용자입력 ②회사페이지 ③플랫폼 best-effort, robots.txt 준수)|
|`fetch_tech_blog`|회사|SourceDocument[] (출처유형=기술블로그)|
|`get_company_values`|회사|SourceDocument[] (출처유형=인재상)|
|`parse_resume`|파일 (OCR 포함)|텍스트 + 구조 힌트|
|`extract_competencies`|SourceDocument, 직군|CompetencyRecord[]|
|`match_competencies`|요구/보유 Record|3단계 판정 결과 + 기준별 근거|
|`build_strategy_brief`|판정 결과, 목표 시점|3트랙 트리아지|

**안전장치**: 도구 호출 예산 상한 / max iteration / 모드별 루프 분리

**기술 배치** (억지 사용 금지)

|기술|자리|v1 채택|
|---|---|---|
|ReAct|소스 수집|✅|
|OCR|이력서 입력|✅|
|RAG|대형 문서 청킹|⚠️ 조건부 (수집 총량이 한계 초과 시에만)|
|MCP|수집 도구 서버화|❌ v2 이관 (v1은 함수로 충분)|

## 9. 상태 (State)

|Layer|내용|수명|
|---|---|---|
|세션|ReAct 궤적, 대화 맥락, 도구 호출 예산 잔량|휘발|
|사용자|역량 프로필 JSON — 회사 무관, 모드를 넘어 재사용|영속 (파일 다운로드/업로드)|

> _**캐시 Layer는 §6 결정에 따라 제거. 사용자 프로필과 수집 결과를 혼합하지 않는다 — 혼합 시 "내 역량"에 타사 JD 문장이 유입된다.**_

## 10. 출력 렌더링 — 지원 전략 브리프

출력은 자유 서술이 아니라 **코드가 골격을 소유하는 고정 스키마**다. 같은 입력이면 같은 골격·계산 결과가 나오도록 하고(멱등성), LLM은 슬롯 안 자연어만 채운다.

**결측 처리 3분류 (혼동 금지 — "없어선 안 될 것이 없다" vs "없어도 되는 게 없다")**:

- **① 필수 _입력_ 부재** (JD·지원 목표 시점·회사/부서 등 §5 필수) → "해당 없음" 아님. **조치**: 수집 재시도 → 실패 시 **사용자에게 요구**(원칙 #6 체크포인트, 본문 3단계 폴백 ①로 연결) → 그래도 없으면 **하드 게이트 강등/차단**(§11).
- **② 개별 역량의 _근거_ 부재** → 라벨링 아님. **카드 미생성**.
- **③ 트랙에 항목 0개** (예: 포기할 것 없음) → 정상 상태이므로 **"해당 없음"** 표기(트랙1은 §7-3 Fallback).

상단에 **지원 목표 시점까지 남은 기간**과 **최소 요건 충족 여부**(§11)를 배지로 표시한 뒤, 세 트랙으로 구성합니다.

|트랙|트랙 분류|내용|재료|
|---|---|---|---|
|1|**지금 내세울 것**|강점 → 이력서·자소서 문구로 번역|레벨이 요구를 상회하는 충족 항목 + 컬처핏 정렬 항목|
|2|**기간 내 채울 것**|남은 기간에 가능한 것만, 순서대로|주로 `인접` 항목 (학습비용 낮음)|
|3|**이번엔 포기할 것**|명시적 배제 + 다음 기회 표시|소요 구간이 남은 기간을 초과하는 `미보유` 항목|

**계약 골격** (■ = 코드가 결정론적으로 채움 · ◇ = LLM 슬롯):

```
[메타 헤더]   ■ 대상 회사·선택 공고 / 남은 기간 / 소스 충족률 % / 미수집 목록 / 신뢰등급(정상|추정 기반)
[요약 판정]   ■ 3-state 집계(충족 N·인접 M·미충족 K) + 산술 등급      ◇ 근거 있는 총평 한 줄
[트랙1 내세울] 카드 ■ 역량명·내 레벨 vs 요구·근거                     ◇ 이력서·자소서 번역 문구
[트랙2 채울]   카드 ■ 역량명·상태(인접/미충족)·요구수준·우선순위·근거   ◇ 보완 방법
[트랙3 포기]   카드 ■ 역량명·(소요 구간 > 남은 기간)·"최종 결정은 사용자" ◇ 권고 사유
[컬처핏]      조건부 — 없으면 "미수집" 표기                          ◇ 번역 + 궁합 고지(공략법 금지)
[공백 고지]   ■ 판단 못 한 항목 + 이유(어떤 소스 부재)
```

각 항목은 카드(`역량명 / 중요도 / 3-state 배지 / 내 레벨 / 기준별 판정·근거(기본 접힘)`)로 렌더링한다. 근거를 모두 펼치면 텍스트 늪이 되므로 기본 접힘. **출력 항목 하나 = UI 카드 하나 = 루브릭 평가 단위 하나**로 일치시킨다. 상단 배지에는 **남은 기간**과 **최소 요건 충족 여부**(§11)를 표시한다.

**트랙1 공백 시 예외 규칙(일반 "해당 없음"과 구분, §7-3)**: 상회 항목이 없다고 해서 미달 항목을 끌어와 강점으로 재라벨링하지 않는다. 대신 고정 문구("이 공고 기준을 상회하는 강점 없음")에 이어, **트랙2에서 이미 계산된 최우선 순위 항목**을 자동으로 함께 표시한다 — 새 판정이 아니라 기존 순위의 인용이므로 근거 규칙을 위반하지 않는다.

우선순위(트랙2)와 포기 판단(트랙3)의 학습 순서는 **절대 시간이 아니라 상대 순위 + 구간(단기/중기/장기)** 으로 제시하며, 근거로는 표준 커리큘럼 시수·강의 분량 등 **출처 있는 숫자만** 인용한다.

**멱등성 트레이드오프 기준**: 구조 경직과 자율형식으로 인한 신뢰도 하락이 부딪히면 **신뢰도를 택한다** — 자율형식을 포기하고 구조를 고정한다. 경직 부작용은 "골격 고정 + 슬롯 텍스트 자유"로 완화한다.

## 11. 실패 처리 (Failure)

> **원칙: 최악의 실패는 멈추는 것이 아니라 그럴듯하게 지어내는 것.**

모든 실패 처리는 "모르면 모른다고 말하고, 어디까지 알아냈는지 밝힌다"로 수렴한다.  
**관통 규칙: 부분 실패를 전체 실패로 만들지 않는다.**

**단, robust함과 쓸만함은 다르다 — 최소 요건(품질 게이트)을 둔다.**

|게이트|기준|미달 시|
|---|---|---|
|UC-2 하드 요건|JD 최소 1건|리포트 미생성, 또는 "추정 기반"으로 등급 강등 + 상단 명시|
|UC-1 하드 요건|프리셋 대분류 커버리지 기준 이상|프로필 `미완성` 표시, UC-2 진입 경고|
|소프트 요건|기술블로그·인재상|진행하되 "미수집" 표기|

| 실패 케이스                                  | 대응                                                                              |
| --------------------------------------- | ------------------------------------------------------------------------------- |
| 회사·부서 미발견                               | 검색어 변형 N회 후 중단. 유사 부서명 후보 제시 후 확인                                               |
| JD 부재 (현재 미채용)                          | 과거 공고 + 블로그로 진행, "현재 공고 없음, 과거 자료 기반" 명시                                        |
| 부서/공고 모호 (예: "백엔드"가 플랫폼·로보틱스·차량SW로 갈라짐) | **핵심 흐름의 사용자 선택 체크포인트로 처리**(§3): 열거된 공고를 그룹핑해 제시하고 사용자가 선택. 임의 선택·병합 금지 (원칙 #6) |
| OCR·파싱 실패                               | 추출 텍스트를 보여주고 직접 보정 기회 제공                                                        |
| 근거 없는 역량 생성                             | 출력에서 제외 (근거 필드가 비면 카드 미생성)                                                      |
| **결과물 기준 미달**                           | **품질 게이트로 차단·강등·표기** (위 표)                                                      |
| 예산·iteration 초과                         | 부분 리포트 생성 + 미커버 영역 명시                                                           |
| 컬처핏 안전선 위반                              | 해당 출력 차단 (§12 게이트를 런타임 적용)                                                      |

## 12. 성공 기준

**루브릭 평가** — 항목 단위, 3점 척도(`부적합 / 부분 적합 / 적합`). 매칭이 기준 단위로 쪼개졌으므로(§7-2), 채점자는 "판정이 타당한가"라는 큰 질문 대신 **기준별 소판정이 맞는가**를 평가한다 → 평가자 간 일치도 상승.

|차원|판정 대상|
|---|---|
|근거 타당성|근거가 주장을 실제로 뒷받침하는가|
|요구역량 정확성|환각·무관 항목이 섞이지 않았는가|
|매칭 판정 타당성|기준 분해와 3-state 집계가 타당한가|
|실행 가능성|학습 순서가 구체적이고 기간 현실적인가|
|**전략 타당성**|강점·보완·제외 트리아지가 남은 기간에 비추어 합리적인가|

**컬처핏 안전성은 점수가 아닌 게이트.** 금지선 위반 시 다른 항목 만점이어도 실패 처리.

**LLM-as-judge**로 규모 확보. 단 사람이 매긴 20~30건과의 일치율을 먼저 검증한 뒤에만 자동 채점을 신뢰. 생성·심사 모델이 같으면 자기 출력을 과대평가하므로 심사자는 다른 모델 또는 사람 앵커 유지.

**자동 과정 지표**: 수집 성공률(≥90%), 근거 첨부율(100%), 최소 요건 충족률, 응답 시간, 도구 호출 횟수 중앙값.

## 13. 열린 항목

|#|내용|
|---|---|
|~~S1~~|**해소됨(D24).** 사람인·원티드·잡코리아·공공데이터 공식 API 존재(비상업 무결). 단 API는 메타+URL만 → 발견=API, 본문=3 Layer Fallback. 잔여 하위 스파이크: 원티드 본문 풍부도·robots.txt·승인 실무(구현 시 실측)|
|S3|실제 컬처핏 시험(현대오토에버 ACT 등) 구성·평가 방식. 추측 반영 금지|
|~~S4~~|**해소됨(D23).** 부트캠프 평가 = **완성도 중심**(제품이 실제로 잘 작동하는가) 확인 → D22 확정: 캐시 제거·RAG 조건부·MCP v2 이관 유지, 점수용 기술 재투입 없음. 추가 의무: 스코프 최소화 + "v1 비사용 근거"를 발표에 명시|
|S5|실패 임계값 구체 수치(검색어 변형 N회, 예산 상한, 커버리지 기준 %) — 실측 후 확정|
|S6|기준 분해 시 "필수 기준 절반 이상 → 인접" 등 집계 규칙의 경계값 — 실측 후 튜닝|