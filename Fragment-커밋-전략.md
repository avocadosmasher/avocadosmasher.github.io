# Fragment 커밋·push 전략

합의일: 2026-09-12. 사용자 요청에 따라 이 전략을 저장하고 이후 개발에 적용한다.

- 개발 브랜치: `feat/fragments`. `main` push는 GitHub Pages 자동 배포를 실행하므로 개발 작업은 이 브랜치에서 보존한다.
- 최초 누적 변경은 검토와 테스트·타입 검사 후 기본 구현 체크포인트 커밋 하나로 남기고 개발 브랜치에 push한다. 전체 기능 완료나 배포 검증 완료를 의미하지 않는다.
- 이후 작은 태스크 하나마다 구현·관련 테스트·개발 기록을 같은 커밋에 포함한다. 관련 검사 통과 후 사용자에게 기능·준비·테스트 방법·예상 결과를 안내하고 판정을 기다린다. 명시적인 ‘통과’ 판정 후에만 커밋하고 작업 마감 시 개발 브랜치에 push한다. ‘수정’ 판정이면 수정·자동 검사·수동 검증을 반복한다.
- 이 사용자 검증 절차는 최초 체크포인트와 main 병합·push에도 적용한다. 상세 절차와 Git 훅 사용법은 `AGENTS.md`를 따른다.
- Red 단계는 로컬에서 확인한다. 실패 상태를 정상 완료 커밋으로 남기지 않는다.
- A03-1(CMS 설정·ID 계약), A03-2(관리 화면·격리 로컬 연결), A03-3(폼 생성·수정 파일 저장 검증)을 각각 나누어 진행한다. 한 턴에는 작은 태스크 하나를 검증·기록하고 멈춘다.
- `main` 병합·push는 해당 배포 범위의 인수 조건과 build·preview 검증을 충족한 후 진행한다. 개발 브랜치 push와 실제 배포를 구분한다.
- 개발 기록에는 실행 명령·결과·남은 제약을 남긴다. 커밋 해시는 다음 기록에서 참조하거나 Git 이력으로 확인한다. 커밋 자체의 해시를 같은 커밋에 넣기 위해 amend를 반복하지 않는다.
- push 실패 시 로컬 커밋을 보존하고 실패 원인을 기록한다. 성공 확인 전 원격 백업 완료라고 표시하지 않는다.

커밋 메시지 예시: `feat(fragments): define CMS config and stable ID contract`.
