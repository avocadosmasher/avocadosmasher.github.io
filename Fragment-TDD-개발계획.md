# Fragment TDD 개발 계획과 작업 목록

작성일: 2026-09-11

현재 상태: 개발 진행 중. 개별 작업 상태와 재개 지점은 `Fragment-개발기록.md`를 기준으로 한다. 이 문서는 구현 완료 보고서가 아니다.

2026-09-17 관계·삭제 피드백 수정: 같은 두 카드 사이에는 방향·유형과 무관하게 관계 하나만 허용한다. 대상 선택과 저장 직전 최신 관계 검사에 적용한다. 기존 관계는 자동 제거하지 않는다. 저장·삭제 전 인증 조회와 오류 안내를 개선했으며 최신 테스트 사이트에서 새 수동 판정을 기다린다.

커밋·push는 [Fragment 커밋 전략](./Fragment-커밋-전략.md)을 따른다. 개발 브랜치에서 검증된 작은 태스크마다 코드·테스트·기록을 함께 커밋하고 push한다. main 반영은 배포 인수 후 별도로 진행한다.

2026-09-11 사용자 요청에 따라 앞으로 한 턴에 작은 태스크 하나를 완료하고 검증·기록 후 멈춘다. 중간 구현은 보존하고 다음 작업으로 자동 확장하지 않는다. PC 재시작 후에는 개발 기록과 git 상태를 먼저 확인한다.

## 목표와 종료 범위

현재 Astro + GitHub Pages 블로그에 Fragments 탭을 추가한다. 웹에서 카드 작성·수정, 카테고리 분류, 검색, 페이지네이션, 카드 팝업, 개념 관계 그래프를 제공한다. 구현·테스트·운영 배포 검증·운영 가이드·최종 보고서 작성까지 완료해야 개발 종료다. 중간 버전에서 그래프나 웹 저장을 생략한 채 전체 완료로 처리하지 않는다.

2026-09-15 사용자 합의로 작성·수정 UX를 변경했다. 기존 블로그 스타일의 Fragments 화면 안에서 새 카드 작성과 상세 팝업의 수정 기능을 제공하고, GitHub Markdown 저장 → 기존 Actions 배포를 연결한다. Decap `/admin/`은 기존 연동 실험용으로 남기며 일상 작성 진입점으로 사용하지 않는다. 검증한 OAuth를 재사용하되 전용 폼의 인증·권한·GitHub 저장·충돌·만료 처리는 별도로 검증한다. 웹 저장이 불가능하면 원인과 대안을 먼저 제시한다.

### 전용 편집 UI 전환 순서

- G01-UI: 블로그 안의 새 카드 폼, 필수/선택 입력, 닫기·재열기·모바일 검증. 이번 단계는 저장 연결 전 미리보기이며 입력은 현재 페이지에서만 유지한다.
- G02/G03-UI: 작성자 인증·권한과 새 카드 저장, 상세 팝업의 수정 진입, 파일·ID 보존. 방문자에게 작성자 전용 동작을 표시하는 정책도 이 단계에서 연결한다.
- G03-LIVE (2026-09-16 추가): 생성·수정 저장 직후 목록·상세·검색·필터·그래프에 반영하고, 새로고침/다른 탭에서도 테스트 저장소의 최신 카드를 읽는다. 실패 시 이전 목록과 재시도 안내를 유지한다.
- G08-UI (2026-09-16 구현·자동 검사 완료, 수동 판정 대기): 로그인 후 저장소 쓰기 권한을 확인한 작성자에게 카드 삭제를 제공한다. 삭제 확인·취소, 최신 파일 버전 확인, 실패/재시도와 목록 반영을 인수한다. 다른 카드에서 참조하면 삭제를 차단하고 참조 카드 제목을 안내한다. 전체 관계를 검사한 버전에서만 삭제를 반영하며 동시 변경은 강제로 덮어쓰지 않는다.
- G04-UI (2026-09-17 구현·자동 검사 완료, 수동 판정 대기): 작성·수정 폼에서 카드 검색, 유형별 관계 추가·제거, 고정 ID 저장과 재열기를 제공한다. 자기 참조·동일 관계 중복을 막고, 관계 저장과 삭제의 동시 변경을 덮어쓰지 않는다. G08-UI의 이전 1~3번은 사용자 통과, 관계 삭제 차단은 검증 불가였으므로 A→B 생성·차단·관계 제거·삭제 흐름을 새로 인수한다.
- G05~G07-UI: 실패 시 입력 보존, 재로그인, 충돌 안내, 다른 세션의 실제 저장 인수. 기존 Decap 테스트 통과를 전용 UI 통과로 대신하지 않는다.
  - G05/G06-UI 재로그인·초안 복구 (2026-09-17 구현·자동 검사 완료, 수동 판정 대기): 저장·삭제가 로그인 만료나 권한 실패로 끊기면 실패 안내 영역에서 바로 다시 로그인하고, 입력한 초안 그대로 같은 작업을 다시 눌러 마무리한다. 안내는 다음에 누를 버튼 이름을 제시하며 재로그인 실패·취소도 사유를 남긴다. 로그인과 무관한 실패에는 재로그인 버튼을 제공하지 않는다. 실제 계정의 토큰 만료 인수와 G07-UI(다른 세션 실제 저장)는 남아 있다.

공개 조회, 작성자만 수정, 페이지당 12개, 주 카테고리 하나와 복수 태그를 기본으로 한다. 검색은 용어·별칭·요약·카테고리·태그에 적용한다. 관계는 직접 지정하며 자동 배치로 시각화한다. AI 관계 추론, 별도 DB, 비공개 카드, 전역 블로그 검색 통합, 이미지 업로드는 이번 필수 범위에 포함하지 않는다.

## TDD 실행 방식

각 동작은 다음 순서로 진행한다.

1. **Red:** 기대 동작을 테스트로 먼저 표현하고 실행한다. 해당 기능이 없거나 잘못되어 실패하는지 확인한다. 의존성 설치 실패·서버 미실행은 유효한 Red로 기록하지 않는다.
2. **Green:** 그 테스트를 통과시키는 최소 구현을 추가한다.
3. **Refactor:** 중복과 책임 분리를 정리하고 해당 테스트와 영향받는 회귀 테스트를 다시 실행한다.
4. **기록:** 작업 ID, 실패/통과 명령과 결과, 수정 파일, 남은 제약을 개발 기록에 남긴다.

한 행은 독립적으로 통과/실패를 판단할 수 있는 작업이다. 행 안에 정상·경계·실패 사례가 여럿 있으면 한 사례씩 Red → Green을 반복한다. 관련 테스트가 통과한 상태에서 작업을 마감한다. 구현 코드의 내부 함수 호출 순서나 CSS 클래스 문자열을 그대로 복제하는 테스트보다 사용자 결과와 데이터 계약을 검증한다.

설정 조사·외부 서비스 연결·문서 작성에는 억지로 실패 테스트를 만들지 않는다. 해당 작업은 사전 정의한 검증 절차와 산출물로 완료를 판정한다. 기존 코드 기준선 테스트는 처음부터 통과할 수 있다. 외부 CMS 자체를 다시 테스트하기보다 우리 스키마·설정·저장소·배포와 만나는 경계를 검증한다.

## 테스트 구성

| 계층 | 도구 제안 | 책임 |
| --- | --- | --- |
| 단위 | Vitest | 스키마, ID/관계 검증, 검색, 정렬, 페이지 계산, URL 상태, 그래프 데이터 |
| 통합 | Vitest + 격리 콘텐츠 fixture + Astro 빌드 | 실제 Markdown 수집, 렌더링, 직렬화, 잘못된 콘텐츠 차단, CMS 저장 포맷 호환 |
| 브라우저 | Playwright | 카드 탐색, 필터, 페이지 이동, 팝업, 키보드, 그래프, CMS 로컬 저장 |
| 실제 연동 | 테스트용 저장소/환경에서 수동 인수 + 자동화 가능한 검사 | GitHub OAuth, 실제 커밋, Actions, 여러 세션의 수정 |
| 배포 인수 | 프로덕션 빌드/preview 및 승인된 실제 배포 확인 | 정적 산출물, 경로, 읽기 화면과 웹 저장의 전체 흐름 |

Astro 공식 문서에서 Vitest와 Playwright 사용을 안내한다. 설치 버전은 현재 프로젝트의 Astro 5.18.2 및 Node 환경과 호환되는 것으로 선정하고 잠금 파일에 고정한다. 최신 Astro 기능 사용이나 프레임워크 업그레이드를 이 작업의 전제로 삼지 않는다.

계획할 명령은 `test:unit`, `test:integration`, `test:e2e`, `test:e2e:preview`, `check`다. 정확한 구성은 A02에서 확정한다. 일반 개발 E2E는 개발 서버로 실행하고 최종 검증은 반드시 build 결과를 preview로 제공해 실행한다. Playwright는 별도 포트와 명시적 URL을 사용하여 사용자가 실행한 서버와 혼동하지 않게 한다.

fixture는 실제 콘텐츠 밖에 두고 임시 프로젝트/복사본에서 사용한다. 0·1·12·13·25개 카드, 한글 조합형/분해형, 영문 별칭, 중복 제목/ID, 끊어진 관계, 긴 설명, HTML처럼 보이는 문자열을 포함한다. 운영 저장소와 배포 사이트에는 자동 테스트 데이터를 기록하지 않는다.

## A. 기준선과 외부 연동 가능성 — 4개

| ID | 작업 | 선행 | 테스트 또는 완료 기준 |
| --- | --- | --- | --- |
| A01 | 기존 동작 기준선 기록 | 없음 | 홈·블로그·태그·소개·검색·공개 글 응답, git 상태, build/check의 기존 실패를 별도로 기록 |
| A02 | 테스트 실행 환경 구성 | A01 | 단위 테스트 1개와 기존 페이지 브라우저 검사 실행. 고의 실패 시 명령이 비정상 종료하는지도 확인 |
| A03 | 웹 저장 구성의 최소 실험 설계·로컬 검증 | A02 | CMS 폼으로 샘플 파일을 격리 폴더에 저장하고 Astro가 읽는지 확인. OAuth 제공자 후보·비용·권한·콜백·필요 설정을 기록 |
| A04 | 실제 인증·GitHub 저장 최소 실험 | A03, 외부 구성 연결 | 테스트 환경에서 로그인 → 샘플 커밋 → 다른 세션에서 읽기 성공. 실패하면 원인을 분류하고 대안 제시 |

A04는 가능하면 초기에 수행해 웹 저장 위험을 먼저 해소한다. 계정 설정 대기 중에는 B~F와 G의 로컬 작업을 진행할 수 있다. 단, A04 미완료 상태는 웹 저장 완료가 아니다.

## B. 카드 데이터 계약 — 5개

| ID | 작업 | 선행 | 먼저 작성할 실패 테스트 / 완료 기준 |
| --- | --- | --- | --- |
| B01 | 필수 필드와 선택 필드 정의 | A02 | id/title/summary/category 누락·공백을 거부하고 aliases/tags/relations 기본값 적용 |
| B02 | 안정적인 ID와 고유성 검증 | B01 | 제목 변경에도 ID 유지, 같은 제목 다른 ID 허용, 중복 ID와 URL에 부적합한 ID 거부 |
| B03 | Fragment 컬렉션 수집 | B02 | 격리 Markdown 카드가 수집되고 기존 blog 컬렉션 결과는 유지됨 |
| B04 | 관계 참조 검증 | B03 | 없는 대상·자기 참조·지원하지 않는 관계 유형을 파일/ID가 포함된 오류로 거부 |
| B05 | 공개 데이터·본문 생성 | B04 | 한글·따옴표가 보존되고 설명의 HTML 유사 문자열이 실행되지 않음. Markdown 보충 설명과 링크가 올바르게 렌더링됨 |

명시적 ID는 생성 후 편집 화면에서 변경하지 못하도록 구성한다. 본문은 짧은 Markdown을 기준으로 한다. ID 생성 방법과 허용 형식은 B02에서 CMS와 동일하게 정의한다.

## C. 탭과 카드 탐색 — 4개

| ID | 작업 | 선행 | 먼저 작성할 실패 테스트 / 완료 기준 |
| --- | --- | --- | --- |
| C01 | Fragments 메뉴와 페이지 연결 | B03 | 메뉴 클릭으로 `/fragments/` 진입, 활성 메뉴 표시, base 경로가 있을 때도 링크 유효 |
| C02 | 카드 목록 렌더링 | C01, B05 | 데이터의 용어·설명·카테고리가 표시되고 카드가 0개일 때 빈 상태 표시 |
| C03 | 카테고리 집계 | B03 | 블로그 설정에 없는 Fragment 카테고리도 나타나고 전체·카테고리별 수가 정확함 |
| C04 | 카테고리 필터 연결 | C02, C03 | 카테고리 선택 시 일치 카드만 표시되고 전체 선택 시 복원 |

## D. 검색·페이지네이션·URL — 5개

| ID | 작업 | 선행 | 먼저 작성할 실패 테스트 / 완료 기준 |
| --- | --- | --- | --- |
| D01 | 검색 정규화·검색 대상 매칭 | B03 | 영문 대소문자·한글 정규화·앞뒤 공백 처리, 5개 검색 필드별 매칭, 빈 검색은 전체 반환 |
| D02 | 검색 우선순위와 필터 결합 | D01, C04 | 용어/별칭 완전 일치 우선, 동점은 안정적 순서, 카테고리와 AND 결합, 결과 없음 표시 |
| D03 | 페이지 계산 | D02 | 0·1·12·13·25개 결과의 페이지 수/항목 범위 정확, 음수·문자열·범위 밖 페이지 정규화 |
| D04 | 검색창·페이지 버튼 연결 | D03, C02 | 2페이지에서 검색/카테고리 변경 시 1페이지 복귀, 이전/다음 경계, 결과 개수와 표시 카드 일치 |
| D05 | URL 저장과 복원 | D04 | q/category/page의 한글 인코딩, 직접 접근·새로고침·뒤로/앞으로 복원. 입력은 replace, 명시적 페이지/필터 이동은 push 등 이력 정책 검증 |

기본 정렬은 용어명, 동점은 ID로 고정한다. 검색 시 완전 일치 우선순위를 먼저 적용한다. 잘못된 카테고리는 전체로, 범위를 벗어난 페이지는 유효 범위로 보정한다. 이 정책을 테스트가 계약으로 고정한다.

## E. 카드 팝업 — 4개

| ID | 작업 | 선행 | 먼저 작성할 실패 테스트 / 완료 기준 |
| --- | --- | --- | --- |
| E01 | 선택 카드 상세 열기 | C02, B05 | 카드 클릭 시 해당 카드의 설명·본문이 dialog에 표시되고 배경이 모달 상태가 됨 |
| E02 | 닫기와 키보드 탐색 | E01 | 닫기 버튼/Escape로 닫기, 포커스가 팝업 내부에 머물고 닫은 뒤 원래 카드로 복원 |
| E03 | 카드 URL와 이력 처리 | E02, D05 | card ID 직접 링크로 열기, 닫아도 검색/페이지 유지, 뒤로가기로 팝업 닫기, 없는 ID는 안내 후 목록 유지 |
| E04 | 관련 카드 이동 | E03, B04 | 관계 목록에서 다른 카드로 이동, 관계 유형 표시, 연결 없는 카드의 빈 상태 처리 |

## F. 그래프 — 5개

| ID | 작업 | 선행 | 먼저 작성할 실패 테스트 / 완료 기준 |
| --- | --- | --- | --- |
| F01 | 노드·간선 데이터 변환 | B04 | 모든 카드 ID가 노드에 매핑, 방향 관계 유지, related/contrasts의 역방향 중복 제거 |
| F02 | 중심 카드와 이웃 선택 | F01 | 중심 카드+직접 이웃만 추출, 고립 노드 유지, 현재 목록 페이지에 없는 이웃도 포함 |
| F03 | 그래프 보기와 지연 로딩 | F02, C01 | 목록만 볼 때 그래프 청크를 요청하지 않고 그래프 전환 시 로딩·렌더링. 실패하면 재시도/카드 보기 제공 |
| F04 | 그래프 조작과 팝업 연결 | F03, E04 | 노드 선택 → 동일 카드 팝업, 확대/축소/초기화, 전체/이웃 전환. 픽셀 좌표 대신 선택 결과 검증 |
| F05 | 그래프와 검색 상태 연결 | F04, D05 | 검색 결과와 필터 밖 이웃 구분, 보기 전환 뒤 검색 유지, 0건·고립 카드 처리 |

그래프 자동 배치의 좌표나 애니메이션 프레임은 테스트 계약으로 삼지 않는다. 그래프와 동일한 관계 탐색을 팝업의 링크 목록으로도 제공한다.

## G. 웹 작성·수정 — 7개

| ID | 작업 | 선행 | 먼저 작성할 테스트 또는 완료 기준 |
| --- | --- | --- | --- |
| G01 | 관리 화면 진입과 폼 계약 | A03, B01 | `/admin/` 접근, 필수/선택 입력과 스키마 일치, 필수값 누락은 저장 전 표시 |
| G02 | 새 카드 생성 | G01, B02 | 격리 로컬 backend에서 폼 저장 → 고유하고 고정된 ID의 Markdown 생성 → 컬렉션 검증 통과 |
| G03 | 기존 카드 수정 | G02 | 제목/설명 수정 시 같은 파일·ID 유지, 중복 카드 생성 안 됨, 다시 열면 수정 내용 유지 |
| G04 | 관계 검색·저장 | G03, B04 | 기존 카드를 검색해 관계 선택, ID로 저장, 다시 열어 관계 복원. 대상 변경/삭제로 끊어진 관계도 검증에서 발견 |
| G05 | 인증·권한 연결 | A04, G01 | 실제 환경에서 작성자 로그인 성공, 로그인 취소·만료·쓰기 권한 없는 계정은 저장 성공으로 처리되지 않음 |
| G06 | 저장 실패·충돌 처리 | G03, G05 | 통제된 응답으로 통신 오류/권한 거부/충돌을 재현해 실패 표시와 입력 보존 확인. CMS 기본 동작이 부족하면 대응 구현과 견적 수정 |
| G07 | 실제 기기 간 저장 인수 (2026-09-20 사용자 통과, 증거는 H03 기록) | G04, G06 | 테스트 저장소에서 생성 → 실제 커밋 확인 → 별도 브라우저 세션에서 수정 → 새 커밋 확인. 관계와 ID도 유지 |

인증 자체는 테스트 계정으로 실제 확인한다. mock 통과를 실제 OAuth 성공으로 대체하지 않는다. 로그인 비밀값은 외부 인증 서비스에 두고 저장소와 정적 산출물에 포함하지 않는다. 로컬 CMS proxy는 격리된 테스트 데이터만 쓰며 운영 설정에 활성화하지 않는다.

2026-09-16 사용자 요청으로 전용 UI의 카드 삭제를 G08-UI에 추가했다. 기존 Decap 삭제 설정은 이 변경과 별개다. 삭제로 관계가 끊어질 수 있으므로 B04 검증을 유지하고, 삭제 시 관계 처리·실패 복구를 인수한다. 잘못된 관계가 저장되더라도 배포 검증이 이를 차단해야 하며, 오류를 고치는 절차를 운영 가이드에 담는다.

## H. 통합·CI·배포 인수 — 5개

| ID | 작업 | 선행 | 테스트 또는 완료 기준 |
| --- | --- | --- | --- |
| H01 | 모바일·테마·접근성 인수 (2026-09-17 구현·자동 검사 완료, 수동 판정 대기) | C~F | 390px/1280px, 밝은/어두운 테마에서 검색·페이지·팝업·그래프 사용. 키보드 흐름과 가로 넘침 확인. `npm run test:h01`(25개 fixture 카드, 포트 4402)로 자동화하고 CI 게이트에 포함. 밝은 테마 `--muted` 대비 4.28:1 → 4.68:1로 수정. 실제 단말·스크린리더는 미확인 |
| H02 | 현실적인 규모의 성능 확인 (2026-09-17 구현·자동 검사 완료, 수동 판정 대기) | F05 | 500개 카드/1,500개 간선 fixture로 검색→화면 갱신과 그래프 열기 측정. 제안 목표: 검색 p95 200ms 이내, 그래프 조작 가능까지 2초 이내. 실행 기기·브라우저·네트워크 조건 기록. `npm run test:h02`(500개 카드·1,500개 간선 fixture, 포트 4403)로 자동화하고 CI 게이트에 포함. 측정값(fcose 최종, 3회): 검색 p95 2.7~2.9ms, 그래프 렌더 881~1,077ms(이 PC, Chromium, 로컬 네트워크). 배치·탐색 UI 결정은 `Fragment-설계-결정기록.md`. 2026-09-19 Actions 러너에서 2,239ms로 실패해, 2초는 개발 PC 목표로 두고 CI 한도는 3초로 조정(H02-CI 기록) |
| H03 | 통합 회귀와 정적 빌드 검증 (2026-09-20 사용자 통과, `721f45a`) | G07, H01, H02 | 신규 핵심 시나리오 및 기존 블로그 회귀 통과, build 성공, preview에서 E2E 통과. 기존 check 오류와 신규 오류 구분 후 배포를 막는 오류 해결 |
| H04 | GitHub Actions 테스트 게이트 (2026-09-17 구현·로컬 검사 완료, 수동 판정 대기) | A02 이후 점진 적용, 최종 H03 | PR과 배포 경로에 검사 연결. 테스트/콘텐츠 검증 실패 시 배포 job 미실행, 성공 시 build 산출물을 사용해 검증 후 배포 가능. 재사용 워크플로 `verify.yml`을 `ci.yml`(PR·개발 브랜치)과 `deploy.yml`(main)이 공유한다. 실제 Actions 실행 결과는 push 이후 확인 |
| P01 | 운영 저장 설정 코드 (2026-09-20 사용자 통과, `38bc406`) | H03 | H03 중 발견: 운영 빌드에는 쓰기 설정이 없어 작성 창이 미리보기로만 동작한다. `FRAGMENT_WRITER_OAUTH_ORIGIN`이 있을 때만 블로그 저장소 `main`으로 저장을 켠다. 설정에 `production: true`를 명시해야 하며, 다른 저장소·브랜치, HTTPS가 아닌 origin, 테스트·로컬 모드와 동시 설정은 거부한다 |
| P02 | 운영 OAuth·배포 연결 (2026-09-20 사용자 통과, `64b74c2`) | P01 | 사용자 결정(2026-09-20): 테스트와 분리된 운영용 Worker와 GitHub OAuth App. `https://avocadosmasher.github.io`만 허용하는 Worker 배포, Client Secret 등록, 배포 워크플로 빌드에 운영 origin 전달 |
| D01 | 초안 브랜치 저장 (2026-09-21 구현·자동 검사 완료, 수동 판정 대기) | H05-FIX | 사용자 요청: 작업을 모았다가 원하는 시점에 한 번 배포. 저장은 `fragments-draft`에만 쓰고 방문자는 `main`을 본다. 로그인하면 초안까지 보인다 |
| D02 | 발행 버튼 | D01 | 초안과 공개본의 차이를 보여 주고, 한 번 눌러 `main`에 합쳐 배포가 한 번만 돌게 한다 |
| D03 | 배포 상태 표시 | D02 | 화면에서 배포 중·완료·실패를 확인할 수 있게 한다 |
| H05 | 실제 배포와 전체 사용자 흐름 인수 (2026-09-20 PR #1 병합·첫 배포 성공, 이후 작성자 편집으로 배포 실패 → H05-FIX 진행 중) | H04, P02, 배포 단계 실행 가능 | 운영 반영 후 웹 생성/수정 → 커밋 → Actions 성공 → 카드 검색/팝업/관계에 반영 확인. 빌드 실패 시 이전 정상 배포가 유지되는 경로는 테스트 환경에서 검증 |

성능 수치는 현재 측정값이 아니라 계획 목표다. A02에서 실행 환경을 고정하고 필요하면 데이터 규모/로딩 방식을 조정한 뒤 근거를 남긴다. 동일한 테스트를 의미 없이 반복하지 않으며 변경·실패·미해결 우려가 있을 때 회귀 범위를 넓힌다.

CI 게이트는 A02 이후부터 가능한 검사를 순차적으로 추가한다. 사용자 콘텐츠의 잘못된 변경이 테스트를 건너뛰고 배포되지 않게 한다. 외부 OAuth 실계정 검사는 모든 CI 실행에 넣지 않고 별도 인수로 관리한다.

## I. 문서와 개발 종료 — 3개

| ID | 작업 | 선행 | 산출물과 완료 기준 |
| --- | --- | --- | --- |
| I01 | 작성·운영 가이드 | G 단계 이후 초안, H05 후 확정 | 웹 로그인/생성/수정/관계 지정, Markdown 대안, 저장과 배포의 차이, 인증 만료·빌드 실패·충돌 복구, 기존 버전 복원 절차 |
| I02 | 최종 코드·요구사항 검토 | H05, I01 | 모든 요구사항을 작업 ID·테스트·실제 결과에 연결. fixture/비밀값/디버그 설정 누락 확인, 미해결 문제의 영향 기록 |
| I03 | 최종 개발 보고서 작성 | I02 | `Fragment-개발-최종보고서.md` 작성. 구현 결과, 구조, 변경 파일, 검증 증거, 배포 정보, 제약, 운영법, 후속 과제를 포함 |

보고서는 계획을 복사하지 않고 실제 결과를 기록한다. 명령/실행 일시/환경, 통과·실패·미실행, Git 커밋과 Actions 실행 링크, 확인 URL을 포함한다. 민감한 인증 정보는 기록하지 않는다. 실제 OAuth·배포 검증이 남아 있으면 보고서는 중간 보고서로 명시하며 전체 완료로 선언하지 않는다.

## 실행 순서와 중간 결과물

1. **기반/연동 위험 확인:** A01~A04. 테스트 환경과 웹 저장 가능성을 먼저 확인한다.
2. **첫 카드 조회:** B01~B05, C01~C04. 파일 하나가 실제 카드로 표시된다.
3. **조회 기능 완성:** D01~D05, E01~E04. 검색·필터·페이징·팝업·URL 복원이 동작한다.
4. **웹 작성 완성:** G01~G07. 관리 화면에서 생성·수정한 파일을 조회 화면과 연결한다.
5. **관계 탐색 완성:** F01~F05. 같은 카드와 관계를 그래프로 탐색한다.
6. **출시와 종료:** H01~H05, I01~I03. 배포 인수 후 최종 보고서를 작성한다.

원래 계획은 총 42개 작업이다. 이후 전용 UI 전환과 G03-LIVE/G08-UI 확장도 완료 판정에 포함한다. 그룹 순서와 별개로 표의 선행 조건이 실제 의존성이다. 독립적인 로컬 작업은 외부 계정 설정 대기 중에도 진행한다. 외부 환경 대기가 인증/배포 성공으로 간주되지는 않는다.

## 완료 판정과 추적

각 작업의 상태는 `대기 → 진행(Red/Green/Refactor) → 검증 완료`로 기록한다. 외부 조건 대기는 `대기(사유)`로 구분한다. 별도의 `Fragment-개발기록.md`에 아래 형식으로 실행 시점부터 기록한다.

```text
작업 ID / 기대 동작 / 상태
Red: 테스트 명령, 예상 원인으로 실패한 결과
Green: 최소 구현, 통과 결과
Refactor: 변경 내용, 회귀 결과
증거: 파일/테스트 이름/실행 환경/관련 커밋 또는 Actions 링크
남은 사항: 없음 또는 구체적인 미검증 항목
```

최종 종료 조건은 42개 작업의 완료, 필수·심화 기능 모두 인수 통과, 실제 웹 작성/수정 및 배포 확인, 운영 가이드와 최종 보고서 완성이다. 테스트를 작성했거나 개발 서버가 켜졌다는 사실만으로 기능 완료를 판단하지 않는다.

기존 32–62시간은 기능 검토 단계 추정이다. 이번 계획에는 상세한 TDD 증거, CI 게이트, 외부 연동 인수, 최종 보고서가 명시되었으므로 같은 범위를 보장하는 확정 견적으로 사용하지 않는다. A 단계 종료 시 실제 CMS 동작과 기존 검사 오류를 기준으로 잔여 작업량을 다시 산정한다.

## 근거 문서

- [기존 기능 검토](./Fragment-기능-검토.md)
- [Astro 테스트 안내](https://docs.astro.build/en/guides/testing/)
- [Playwright 테스트 서버 설정](https://playwright.dev/docs/test-webserver)
- [Decap CMS GitHub backend](https://decapcms.org/docs/github-backend/)
- [Decap CMS 관계 입력](https://decapcms.org/docs/widgets/relation/)
